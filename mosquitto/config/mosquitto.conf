# MQTT (ESP32·백엔드 PubSub 등)
listener 1883 0.0.0.0
protocol mqtt

# MQTT over WebSockets (브라우저·프록시/HTTPS 환경에서 쓸 때)
listener 9001 0.0.0.0
protocol websockets

allow_anonymous true

# R모듈 압축 JPEG(base64) 포함 status 메시지용 — 약 1.2MB 원문까지 허용
max_packet_size 2097152

persistence true
persistence_location /mosquitto/data/

log_dest stdout
