services:
  backend:
    restart: always
    container_name: meter-backend
    build: ./backend
    env_file:
      - ./backend/.env.production
    depends_on:
      - mosquitto
    volumes:
      # 모듈2 스냅샷 — 재배포에도 유지되어야 하므로 명명 볼륨 사용
      - backend_uploads:/backend/uploads
    networks:
      # MQTT 는 meter_internal 만 사용 — global_network 의 다른 mosquitto 와 혼동 방지
      - meter_internal
      - global_network

  mosquitto:
    image: eclipse-mosquitto:2
    container_name: meter-mosquitto
    restart: always
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    networks:
      meter_internal:
        aliases:
          - meter-mosquitto
          - meter-mqtt
      global_network: {}

  frontend:
    restart: always
    container_name: meter-frontend
    build:
      context: ./frontend
      args:
        VITE_GOOGLE_CLIENT_ID: ${VITE_GOOGLE_CLIENT_ID}
        VITE_KAKAO_API: ${VITE_KAKAO_API}
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    networks:
      - global_network

networks:
  meter_internal:
    driver: bridge
  global_network:
    external: true

volumes:
  mosquitto_data:
  mosquitto_log:
  backend_uploads:
